# legacy/

历史遗留文件，与当前主流程无关，仅为保留参考：

| 文件 | 说明 |
|------|------|
| `rewriter` / `rewriter-hermes` | Linux 下的 bash 启动器（硬编码 `/home/lex/...` 路径），用于启动 `tui/` 的 Ink TUI |
| `agui_to_hermes_gateway.py` | AG-UI → Hermes 协议网关（配合 rewriter-hermes 使用） |
| `search_aof.py` | 一次性的 Google 搜索实验脚本（与论文重写无关） |
